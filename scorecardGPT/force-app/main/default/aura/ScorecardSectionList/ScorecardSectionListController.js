({
    onInit: function(component, event, helper){
        helper.onInit(component);
        helper.getScorecardname(component);
    },

    onSectionMenuClick: function(component, event, helper){
        helper.onSectionMenuClick(component, event);
    }
})